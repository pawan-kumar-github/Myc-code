#include<stdio.h>
int main()
{
	int n,sum=0,a,temp;
	printf("enter number");
	scanf("%d",&n);
	temp=n;
	while (n>0)
	{
		a=n%10;
		sum=sum+(a*a*a);
		n=n/10;
	}
	if (sum==temp)
	printf("Armstrong's no.");
	else
	printf("not Armstrong's number");
}
