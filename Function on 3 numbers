#include<stdio.h>
int max(int a, int b, int c)
{
	if (a>b && a>c)
	return a;
	else if (b>a && b>c)
	return b;
	else
	return c;
}
int main()
{
	int x,y,z;
	printf("enter thr no.");
	scanf("%d%d%d",&x,&y,&z);
	int ans=max(x,y,z);
	printf("the grater no. is ");
	printf("%d",ans);
	
}
